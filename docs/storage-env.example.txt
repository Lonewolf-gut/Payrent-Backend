# =============================================================================
# File storage (KYC, financing docs, property images)
# =============================================================================
# local = dev (private files in storage/private/, public in public/uploads/)
# s3    = production (AWS S3, Cloudflare R2, DigitalOcean Spaces)
STORAGE_DRIVER="local"

# AWS S3 / R2 / Spaces (required when STORAGE_DRIVER=s3)
S3_BUCKET=""
S3_REGION=""
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""
# Optional: required for R2/Spaces (e.g. https://<account>.r2.cloudflarestorage.com)
S3_ENDPOINT=""
# Optional CDN/base URL for public property images and profile photos
S3_PUBLIC_URL=""

FILE_MAX_SIZE_MB="10"
FILE_SIGNED_URL_TTL_SECONDS="900"

# See docs/cloud-storage-setup.md for full cloud setup guide
