import { BackendApiLanding } from "@/components/backend/api-landing";

/** PayRent-Backend split repo: no marketing imports — safe for production build. */
export default function RootPage() {
  return <BackendApiLanding />;
}
